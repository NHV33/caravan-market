<div class="container d-flex w-50 flex-column pt-3">
<h1>My Bookings</h1>

<% if @my_bookings.present? %>
  <ul>
    <% @my_bookings.each do |booking| %>
     <div class="card mb-3 shadow-sm" style="max-width: 100%;">
    <div class="row g-0">

      <div class="col-md-4 temporary-vehicle" style="background: url(<%= asset_path 'default_caravan.png' %>); background-size: contain; background-position: center; background-repeat: no-repeat; background-color: white;">
      </div>

        <div class="col-md-8 px-3">
          <div class="card-body">

            <h5 class="card-title text-capitalize"><%= booking.vehicle_id %></h5>
            <p class="card-text"><%= booking.vehicle_id %></p>
            <p class="card-text"><small class="text-body-secondary"><%= "Provider: #{booking.vehicle_id}" %></small></p>
            <p class="card-text"><small class="text-body-secondary"><%= "Daily Rate: #{booking.vehicle_id}" %></small></p>

          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
  </ul>
<% else %>
  <p>No bookings found.</p>
<% end %>
</div>
